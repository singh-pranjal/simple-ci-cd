pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Pipeline running from Jenkins server"
                sh 'hostname'
            }
        }
    }
}
